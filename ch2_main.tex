\documentclass[11pt,a4paper]{article}

\usepackage[margin=1in]{geometry}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{caption}
\usepackage{subcaption}
\usepackage{longtable}
\usepackage{colortbl}
\usepackage{array}
\usepackage{tabularx}
\usepackage{anyfontsize}
\usepackage{multirow}
\usepackage{float}
\usepackage[section]{placeins}
\usepackage{url}
\usepackage{lineno}
\usepackage[dvipsnames]{xcolor}
\usepackage[super,sort&compress,comma,numbers]{natbib}

\graphicspath{{./images/}}
\captionsetup{font=small,labelfont=bf,labelsep=period}
\captionsetup[subfigure]{font={normalsize,sf},labelfont=bf}
% Author-review layout settings are centralized here rather than in QMD metadata.
\renewcommand{\textfraction}{0}
\renewcommand{\bottomfraction}{1}
\renewcommand{\topfraction}{1}
\renewcommand{\floatpagefraction}{1}
\setlength{\parindent}{0pt}
\setlength{\parskip}{0pt}
\setlength{\emergencystretch}{3em}

% Editorial notes remain visible while the manuscript is being prepared.
\newcommand{\tom}[1]{{\textcolor{RedOrange}{\textbf{[Tom: #1]}}}}
\newcommand{\hh}[1]{{\textcolor{ForestGreen}{\textbf{[Heike: #1]}}}}
\newcommand{\heike}[1]{\hh{#1}}
\newcommand{\agentformat}[1]{{\textcolor{Purple}{\textbf{[Agent: #1]}}}}
\newcommand{\agentreview}[1]{{\textcolor{RoyalBlue}{\textbf{[Agent: #1]}}}}
\newcommand{\agentremovedreview}[1]{{\textcolor{BrickRed}{\textbf{[Agent: #1]}}}}
\newcommand{\agentrevision}[1]{{\textcolor{TealBlue}{\textbf{#1}}}}

% Scientific Data asks authors not to supply internal hyperlink markup.
\newcommand{\href}[2]{#2}
\newcommand{\hyperlink}[2]{#2}
\newcommand{\hypertarget}[2]{#2}
\newcommand{\JournalTitle}[1]{#1}


\begin{document}
\linenumbers

\begin{center}
{\LARGE\bfseries Three-dimensional data of wire-cut surface scans under
the confocal microscope\par}
\vspace{1em}
{\large
\mbox{Yuhang Lin\textsuperscript{1,2,*}}, %
\mbox{Heike Hofmann\textsuperscript{2,3}}, %
\mbox{Curtis Mosher\textsuperscript{4}}, %
\mbox{Eden Amin\textsuperscript{5}}, %
\mbox{Jeff Salyards\textsuperscript{2}}, %
\mbox{Alicia Carriquiry\textsuperscript{1,2}}\par}
\vspace{0.75em}
{\small
\textsuperscript{1}Department of Statistics, Iowa State
University, USA\\
\textsuperscript{2}Center for Statistics and Applications in Forensic
Evidence, Iowa State University, USA\\
\textsuperscript{3}Department of Statistics, University of
Nebraska-Lincoln, USA\\
\textsuperscript{4}Roy J. Carver High Resolution Microscopy
Facility, Iowa State University, USA\\
\textsuperscript{5}Forensic Science Institute, University of Central
Oklahoma, USA\\\textsuperscript{*}Corresponding author: Yuhang
Lin (yhlin@iastate.edu)
\par}
\end{center}
\vspace{1em}

\begin{abstract}
Wire-cut striation marks provide evidence for forensic source
identification, but publicly available datasets and reproducible
quantitative workflows for analyzing these marks are limited. We present
a dataset of 120 three-dimensional topographic scans in \texttt{x3p}
format from aluminum wires cut with five KWS-105 wire cutters. The study
contains six scans---two replicates at each of three cutting
locations---for each of the 20 tool-edge combinations. We provide
additional scan metadata, manually extracted profiles, processed
signals, cross-correlation function (CCF) values for pairwise aligned
signals, images, and analysis code. The accompanying workflow covers
scan inspection, profile and signal extraction, alignment, pairwise
similarity visualization, CCF distributions for same-source and
different-source comparisons, and variability assessment across
replicates, stagings, and acquisitions. The dataset and workflow support
the development, evaluation, and reproducible comparison of quantitative
methods for wire-cut evidence.
\end{abstract}

\agentformat{Remove the table of contents before preparing the clean Scientific Data submission. The journal's Data Descriptor outline does not include a table of contents; this one is included only as an internal review aid.}

\tableofcontents

\section*{Background \& Summary}\label{sec-background-summary}
\addcontentsline{toc}{section}{Background \& Summary}

In forensic analysis, marks left at crime scenes play an important role,
and forensic examiners are particularly interested in identifying their
origin, a process known as the Source Identification Problem in Forensic
Science \citep{lindleyProblemForensicScience1977}. The forensic science
community focuses on two distinct problems: the \textbf{specific source
problem}, in which examiners aim to determine whether a mark was left by
a particular tool, and the \textbf{common source problem}, in which
examiners aim to determine whether two marks were left by the same tool.
Currently, accepted practice for both problems relies on visual
inspection of physical items under a comparison microscope. Examiners
then report their conclusions according to the Theory of Identification
\citep{afteAssociationFirearmTool1998}, developed by the Association of
Firearm and Toolmark Examiners (AFTE), using one of three categories:
\emph{identification}, in which the marks are believed to have been made
by the same source; \emph{elimination}, in which the marks are believed
to have been made by different tools; and \emph{inconclusive}, in which
the similarities or dissimilarities between marks are insufficient to
support either identification or elimination. This process has been
criticized by the National Research Council (NRC)
\citep{nationalresearchcouncilu.s.StrengtheningForensicScience2009a} and
the President's Council of Advisors on Science and Technology (PCAST)
\citep{presidentscouncilofadvisorsonscienceandtechnologyForensicScienceCriminal2016}
for its lack of objectivity and absence of quantifiable measurements,
such as error rates.

In discussing error rates, Biedermann
\citep{biedermannStrangePersistenceSource2022} differentiates between
internal and external perspectives. The external perspective allows
general statements based on black-box studies that relate examiners'
conclusions to ground truth; however, it does not consider the evidence
in a particular case. Introducing an internal perspective requires
evaluating similarity between pairs of specimens with known ground
truth. Algorithms allow these similarities to be quantified objectively
and reproducibly.

Advances in microscopy have made it possible to move from optical
two-dimensional imaging to digital three-dimensional topographic
scanning, allowing algorithmic assessment of the similarity between
pairs of forensic pattern evidence
\citep{bonfantiVisualisationConfocalMicroscopy2000}. This transition has
already supported practical advances in bullet and cartridge
comparisons, including visual inspection of bullet line scans from
three-dimensional topographic images
\citep{smithLinescanImagingBallistics2002}, the use of features
extracted by fast Fourier transform (FFT) as the basis of an artificial
intelligence system \citep{liBallisticsProjectileImage2006}, an
automated pilot study based on consecutively matching striae
\citep{chuPilotStudyAutomated2010} (a feature also used by some
laboratories in casework), and the incorporation of three-dimensional
imaging into routine casework at the Alabama Department of Forensic
Sciences (ADFS) \citep{mcclarinAddingObjectiveComponent2015}.

When cut wires are found at a crime scene, understanding the
characteristics of marks left by bladed tools becomes essential. When a
blade cuts a wire, it leaves striations on the surface, as shown in
Figure~\ref{fig-cut-microscope}. These striations provide a basis for
similarity assessment and source identification. Similar scans have
supported the development of quantitative methods for analyzing firearms
and other toolmark evidence. For example, studies of screwdriver
striations have examined key factors affecting comparisons, including
angles of attack \citep{baikerToolmarkVariabilityQuality2015a},
rotational axes \citep{garciaInfluenceAxialRotation2017}, and cutting
directions \citep{cuellarAlgorithmForensicToolmark2024}. Foundational
research on forensic toolmarks has also emphasized data collection and
analysis, including the collection and distribution of datasets for
bullet and toolmark analysis
\citep{maNISTBulletSignature2004, zhengNISTBallisticsToolmark2016},
numerical methods for improving accuracy and consistency
\citep{chuAutomaticIdentificationBullet2013, vorburgerApplicationsCrosscorrelationFunctions2011},
and techniques for quantifying similarity
\citep{hareAutomaticMatchingBullet2017, juOpenSourceImplementationCMPS2022}.

\begin{figure}[hbt]

\centering{

\includegraphics[width=4.16667in,height=\textheight,keepaspectratio]{images/kaiweet-sketch.png}

}

\caption{\label{fig-cut-microscope}Microscopic wire-cut striations.
Close-up of striations left by a blade on the cut end of a wire; the
schematic identifies the ridge separating the two roof-like surfaces
scanned separately as scans A and B.}

\end{figure}%

Data collected at crime scenes lack the known ground truth needed to
estimate error rates; therefore, controlled studies are needed. In this
study, we provide a publicly available dataset of wire-cut scans
together with a systematic quantitative processing and comparison
workflow. The shared materials include raw scans, metadata, manually
extracted profiles, processed signals, pairwise cross-correlation
function (CCF) values, derived images, and analysis code.

The Methods section describes sample collection, scanning, profile
extraction, signal processing, and CCF-based alignment. Data Records
describes the shared files and repository organization. Data Overview
presents descriptive summaries of pairwise CCF values. Technical
Validation assesses signal-processing and alignment behavior and
variability across replicates, stagings, and acquisitions. Usage Notes
provides practical guidance for working with the shared files, and Code
Availability identifies the custom processing and validation code. The
dataset and workflow may support future quantitative research on
wire-cut evidence.

\section*{Data Acquisition and Processing}\label{sec-methods}
\addcontentsline{toc}{section}{Data Acquisition and Processing}

Five Kaiweets KWS-105 multipurpose tools with 0.5-inch carbon-steel
cutting blades were used to cut 16-gauge anodized pure-aluminum wire.

While aluminum is not commonly used for electric wiring because of its
poor conductivity, it was selected for its nontoxicity and suitability
for receiving cutting marks and retaining them during subsequent
handling. Pure aluminum has a Brinell hardness of approximately 15 HB,
which places it between lead (approximately 5 HB) and copper
(approximately 35 HB).

\subsection*{Scan acquisition}\label{sec-cutting-wires}
\addcontentsline{toc}{subsection}{Scan acquisition}

The blades of each of the five wire cutters were labeled A, B, C, and D
(see Figure~\ref{fig-cut-tent-scan} (a)). B opposes A; the reverse sides
are labeled D for A and C for B. Three locations on each blade were
marked (inner, middle, and outer), as shown in
Figure~\ref{fig-cut-tent-scan} (a). Each four-inch wire segment was cut
in half, creating two cut sides, AB and CD. The cut surface of the wire
is not flat; under the microscope, it reveals a roof-like structure with
a ridge separating the markings made by each blade (see
Figure~\ref{fig-cut-microscope}). The two roof sides of each cut surface
were scanned separately, yielding scans A and B from cut side AB and
scans C and D from cut side CD. As an example, the four scans resulting
from cut 1 by tool 1 are shown in Figure~\ref{fig-cut-tent-scan} (c).

The cut surfaces were scanned at 20\(\times\) magnification using a
Sensofar confocal microscope, shown in Figure~\ref{fig-cut-tent-scan}
(b). The scans were exported in \texttt{x3p} format (ISO
25178-72:2017/Amd 1:2020) with a lateral resolution of 0.645 \(\times\)
0.645 micrometers per pixel and a vertical resolution of 0.1 micrometer.

The complete design therefore consists of 5 tools \(\times\) 3 locations
\(\times\) 2 replicates \(\times\) 4 cut surfaces = 120 scans.

\begin{figure}[hbt]

\begin{minipage}[t]{0.29\linewidth}

\vspace{0pt}

\centering{

\includegraphics[width=\linewidth,height=0.16\textheight,keepaspectratio]{images/plier-tip.png}

}

\subcaption{\label{fig-plier-tip}}

\centering{

\includegraphics[width=\linewidth,height=0.16\textheight,keepaspectratio]{images/wire-microscope-062524.JPG}

}

\subcaption{\label{fig-wire-microscope}}

\end{minipage}%
%
\begin{minipage}[t]{0.02\linewidth}
~\end{minipage}%
%
\begin{minipage}[t]{0.69\linewidth}

\vspace{0pt}

\centering{

\includegraphics[width=\linewidth,height=0.4\textheight,keepaspectratio]{images/T1AW-LI-R2-4edges.png}

}

\subcaption{\label{fig-T1AW-LI-R2-4edges}}

\end{minipage}%

\caption{\label{fig-cut-tent-scan}Wire-cut sampling and scanning
workflow. (a) A Kaiweets KWS-105 wire cutter with inner, middle, and
outer locations marked. (b) A confocal microscope used to scan wire-cut
surfaces. (c) Four scans of cutting marks labeled according to the
corresponding cutting edges A, B, C, and D.}

\end{figure}%

The naming of each piece included the specific tool, its edge, location,
and replicate according to the following convention:

\begin{samepage}
\begin{verbatim}
T__W-L_-R_
 ||   |  |
 ||   |  +-- Replicate: 1 or 2
 ||   +----- Location: I (inner), M (middle), or O (outer)
 |+--------- Edge: A, B, C, or D
 +---------- Tool: 1, 2, 3, 4, or 5
\end{verbatim}
\end{samepage}

For example, T1AW-LI-R1 denotes the first replicate of a piece cut with
edge A of tool 1 at the inner location.

To minimize errors during data collection, two team members cut and
labeled the wire pieces together. One team member scanned the wire cuts
and assigned names to the scans as described.

\subsection*{Signal extraction}\label{sec-extract-profiles}
\addcontentsline{toc}{subsection}{Signal extraction}

Signals were extracted from each surface scan using a two-step
procedure. First, a representative profile was identified along a line
across the surface perpendicular to the dominant striation direction.
This line was chosen manually to avoid scanning artifacts (e.g., holes)
and cutting artifacts (e.g., rips caused by rolling the wire). Along
this line, surface values were assigned at equally spaced positions
using the observed surface value at the nearest position on the surface
grid.

\begin{figure}[hbt]

\begin{minipage}[t]{0.45\linewidth}

\vspace{0pt}

\centering{

\includegraphics[width=\linewidth,height=0.37\textheight,keepaspectratio]{images/T1AW-LI-R1-profiles.png}

}

\subcaption{\label{fig-T1AW-LI-R1-profiles}}

\end{minipage}%
%
\begin{minipage}[t]{0.55\linewidth}

\vspace{0pt}

\centering{

\includegraphics[width=\linewidth,height=0.16\textheight,keepaspectratio]{images/T1AW-LI-R1-profiles-plot.png}

}

\subcaption{\label{fig-T1AW-LI-R1-profiles-plot}}

\centering{

\includegraphics[width=\linewidth,height=0.16\textheight,keepaspectratio]{images/T1AW-LI-R1-signals-plot.png}

}

\subcaption{\label{fig-T1AW-LI-R1-signals-plot}}

\end{minipage}%

\caption{\label{fig-T1AW-LI-R1-profiles-signals}Profile and signal
extraction. (a) A yellow profile line drawn across the scan striations.
(b) The profile function extracted along the line in (a). (c) Raw and
filtered signals obtained from the profile function in (b).}

\end{figure}%

In a second step, we extract the scan's signal by removing trends and
outliers from the surface values of the profile with the help of two
LOWESS filters with bandwidths of \(400 \mu m\) and \(40 \mu m\),
respectively \citep{clevelandLocalRegressionModels1992}. All smoothers
are susceptible to boundary issues, particularly when outliers are
present. Steep changes at the end of a signal have a high potential to
attract another signal during alignment, causing misalignments. To avoid
potential artificial ``hooks'\,' at either end of a signal, two-sample
\(t\)-tests were applied to the surface values in the outermost 2.5\%
intervals along the horizontal axis. Specifically, the summary
statistics (sample size, mean, and standard deviation) of the signal
values within the intervals \([0, 2.5)\%\), \([2.5, 5)\%\),
\((95, 97.5]\%\), and \((97.5, 100]\%\) were calculated. At each end,
the test compared the outermost 2.5\% interval with the adjacent
interval. The signal values in the outermost interval were retained only
when the corresponding \(p\)-value was above 0.01, indicating no
significant shift.

The widths of these intervals correspond to the choice of bandwidths and
the length of the signal. The interval widths were determined using
cross-validation.

The resulting signals are used for subsequent comparisons with other
signals.

Figure~\ref{fig-T1AW-LI-R1-profiles-signals} shows a scan on the left
(a) with the location of the manually chosen profile line in yellow. The
corresponding profile and signal are plotted in (b) and (c).

One profile for each scan is included in folder \texttt{profiles/} as a
csv file in the repository using the same naming convention as the
scans, with the precursor of \texttt{profiles-}. More details on the
exact format of these files are provided below.

A single file containing all 120 signals is available in
\texttt{data-derived/wire-signals.csv.zip}.

\subsection*{Pairwise alignment of signals}\label{sec-align-signals}
\addcontentsline{toc}{subsection}{Pairwise alignment of signals}

Before signals from different scans can be compared, they need to be
aligned. For each pair of signals, the value of the cross-correlation
function (CCF) is calculated across relative shifts. The shift
corresponding to the maximum CCF is retained as the alignment position,
while the maximized cross-correlation function, \(CCF_{max}\), gives a
measure of the similarity of the signals, with a value of 1 indicating
complete similarity (apart from linear scaling) and a value of 0
indicating (statistical) independence.

\begin{figure}[hbt]

\centering{

\includegraphics[width=5.20833in,height=\textheight,keepaspectratio]{images/signals_pair_alignment.png}

}

\caption{\label{fig-signals-pair-alignment}Same-source signal alignment.
Rows correspond to cut surfaces A, C, B, and D. The first two columns
show signals extracted from scans corresponding to the replicate cuts of
tool 1 at location I; the third column overlays the aligned signals and
reports their CCF values.}

\end{figure}%

Figure~\ref{fig-signals-pair-alignment} shows all signals corresponding
to cuts 1 and 2 made with tool 1 at the location closest to the pivot
area (inner location). Each of the four edges corresponds to one row in
the figure. The lines in the first two columns correspond to the
extracted signals for cuts 1 and 2, respectively. The third column shows
the two signals aligned at the location of the maximal CCF. The
cross-correlation value for each pair is included as part of the title
of the chart.

The file \texttt{data-derived/wire\_pairwise\_ccf.csv} contains the CCF
similarity scores of all pairwise aligned signals.

\section*{Data Records}\label{sec-data-records}
\addcontentsline{toc}{section}{Data Records}

\agentrevision{The repository record on Iowa State University DataShare [DATASET CITATION] at \url{https://doi.org/[DOI]} contains the \texttt{x3p} scans, metadata, extracted profiles, processed signals, pairwise CCF values, and derived images.}
\agentformat{Replace the dataset citation and DOI placeholders.
For first-round review, the destination must permit anonymous download; from the second round onward, it must be a public formal repository record.
Add the full dataset reference to References.}

The repository contents, formats, and folder organization are summarized
in Table~\ref{tbl-data-overview}.
\agentreview{The \texttt{variability-assessment/} files have not yet been updated to the current naming conventions.
Recheck the corresponding inventory entry and manuscript descriptions after those files are revised.}

\begin{table}[hbt]

\caption{\label{tbl-data-overview}Structure of available data and
files.}

\centering{

\begin{tabular*}{0.97\linewidth}{@{\extracolsep{\fill}}>{\raggedright\arraybackslash}p{\dimexpr 0.32\linewidth -2\tabcolsep-1.5\arrayrulewidth}|>{\raggedright\arraybackslash}p{\dimexpr 0.5\linewidth -2\tabcolsep-1.5\arrayrulewidth}>{\raggedright\arraybackslash}p{\dimexpr 0.18\linewidth -2\tabcolsep-1.5\arrayrulewidth}}
\toprule
Data type & Description & Section \\ 
\midrule\addlinespace[2.5pt]
\multicolumn{3}{>{\raggedright\arraybackslash}m{0.97\linewidth}}{\parbox{\linewidth}{Raw data}} \\[2.5pt] 
\midrule\addlinespace[2.5pt]
\texttt{scans/} & folder containing 120 topographic 3D scans corresponding to 30 aluminum wire cuts (\texttt{x3p} format) & Scan acquisition \\ 
\texttt{meta.csv} & metadata for each scan: identifier, file path, tool, edge, location, replicate, and pixel dimensions (CSV format) & Scan acquisition \\ 
\midrule\addlinespace[2.5pt]
\multicolumn{3}{>{\raggedright\arraybackslash}m{0.97\linewidth}}{\parbox{\linewidth}{Manual derivatives}} \\[2.5pt] 
\midrule\addlinespace[2.5pt]
\texttt{profiles/} & folder containing one manually extracted profile per scan (CSV format) & Signal extraction \\ 
\midrule\addlinespace[2.5pt]
\multicolumn{3}{>{\raggedright\arraybackslash}m{0.97\linewidth}}{\parbox{\linewidth}{Computational derivatives in the folder 'data-derived/'}} \\[2.5pt] 
\midrule\addlinespace[2.5pt]
\texttt{wire-signals.csv.zip} & profile values and derived signals for all 120 scans (CSV format, in \texttt{wire-signals.csv.zip}) & Signal extraction \\ 
\texttt{wire\_pairwise\_ccf.csv} & CCF values for all pairwise aligned signals (CSV format) & Pairwise alignment of signals \\ 
\midrule\addlinespace[2.5pt]
\multicolumn{3}{>{\raggedright\arraybackslash}m{0.97\linewidth}}{\parbox{\linewidth}{Image files}} \\[2.5pt] 
\midrule\addlinespace[2.5pt]
\texttt{pngs/} & folder containing images of 3D wire-cut scans (PNG format) & Scan acquisition \\ 
\texttt{profile-images/} & folder containing images of profiles extracted from wire cuts (PNG format) & Signal extraction \\ 
\midrule\addlinespace[2.5pt]
\multicolumn{3}{>{\raggedright\arraybackslash}m{0.97\linewidth}}{\parbox{\linewidth}{Technical validation}} \\[2.5pt] 
\midrule\addlinespace[2.5pt]
\texttt{variability-}
\texttt{assessment/} & folder containing technical-validation documents, pending an update to the current naming conventions & Technical Validation \\ 
\bottomrule
\end{tabular*}

}

\end{table}%

\agentrevision{A data dictionary defining non-obvious columns in \texttt{meta.csv}, the profile and signal files, and the pairwise-CCF file is provided in [repository documentation path].
The dataset is available under the [license] license.}
\agentformat{Verify Table 1 against the deposited record and replace the documentation-path and data-license placeholders.
For this non-sensitive original dataset, use CC0 or CC BY rather than a license containing NC or SA restrictions.}

\hh{We need to include a descriptor table for each of the csv files; I started with meta.csv - the formatting is still horrific}

The \texttt{meta.csv} file contains one record for each of the 120
scans, with eight variables described in Table~\ref{tbl-meta-structure}.

\begin{table}[hbt]

\caption{\label{tbl-meta-structure}Structure of \texttt{meta.csv}.}

\centering{

\begin{tabularx}{\linewidth}{llX}
\toprule
Variable & Type & Description \\
\midrule
\texttt{ID} & text & Unique scan identifier defined by the naming convention in Scan acquisition \\
\texttt{x3p\_path} & text & Path to the \texttt{x3p} scan relative to the data repository root \\
\texttt{tool} & integer & Cutting tool, one of 1 through 5 \\
\texttt{edge} & character & Cutting edge, one of A, B, C, or D \\
\texttt{location} & character & Cutting location: I (inner), M (middle), or O (outer) \\
\texttt{replicate} & integer & Replicate cut, either 1 or 2 \\
\texttt{height} & integer & Height of the scan in pixels \\
\texttt{width} & integer & Width of the scan in pixels \\
\bottomrule
\end{tabularx}

}

\end{table}%

The \texttt{profiles/} folder contains 120 CSV files, one manually
extracted profile per scan, with one sampled point per row. Their four
variables are described in Table~\ref{tbl-profile-structure} in CSV
column order.

\begin{table}[hbt]

\caption{\label{tbl-profile-structure}Structure of the
\texttt{profiles-*.csv} files.}

\centering{

\begin{tabularx}{\linewidth}{llX}
\toprule
Variable & Type & Description \\
\midrule
\texttt{ID} & text & Source scan identifier, matching \texttt{ID} in \texttt{meta.csv}; constant within each file \\
\texttt{x} & numeric & Horizontal coordinate along the profile in micrometers, starting at zero in increments of 0.645 \\
\texttt{y} & numeric & \texttt{y} coordinate, constant and set to zero \\
\texttt{value} & numeric & Observed surface height in micrometers; \texttt{NA} denotes a missing value \\
\bottomrule
\end{tabularx}

}

\end{table}%

The \texttt{data-derived/wire-signals.csv.zip} archive contains
\texttt{data-derived/wire-signals.csv}, with one sampled profile point
per row for all 120 scans. Its 12 variables are described in
Table~\ref{tbl-signal-structure} in CSV column order.

\begin{table}[hbt]

\caption{\label{tbl-signal-structure}Structure of the signal CSV in
\texttt{wire-signals.csv.zip}.}

\centering{

\begin{tabularx}{\linewidth}{llX}
\toprule
Variable & Type & Description \\
\midrule
\texttt{ID} & text & Source scan identifier, matching \texttt{ID} in \texttt{meta.csv} and the profile files \\
\texttt{tool} & integer & Cutting tool, one of 1 through 5 \\
\texttt{edge} & character & Cutting edge, one of A, B, C, or D \\
\texttt{tooledge} & text & Combined tool and edge label, for example, \texttt{T1AW} \\
\texttt{location} & text & Cutting location: LI (inner), LM (middle), or LO (outer) \\
\texttt{replicate} & integer & Replicate cut, either 1 or 2 \\
\texttt{x} & numeric & Horizontal coordinate along the profile in micrometers, starting at zero in increments of 0.645 \\
\texttt{y} & numeric & \texttt{y} coordinate, constant and set to zero \\
\texttt{value} & numeric & Observed surface height in micrometers \\
\texttt{detrend} & numeric & Surface height after subtracting the fitted linear trend in \texttt{value} versus \texttt{x}, in micrometers \\
\texttt{raw\_sig} & numeric & Residual in micrometers after removing the 400-micrometer LOWESS trend from \texttt{detrend}, before final smoothing and boundary exclusion \\
\texttt{sig} & numeric & Signal after 40-micrometer smoothing of \texttt{raw\_sig} and boundary exclusion, in micrometers \\
\bottomrule
\end{tabularx}

\agentreview{Confirm whether location values should be standardized across \texttt{meta.csv} (I, M, O) and the signal and pairwise-CCF files (LI, LM, LO).}

}

\end{table}%

The \texttt{data-derived/wire\_pairwise\_ccf.csv} file contains one row
for each ordered pair of the 120 signals, for 14,400 rows in total. Its
19 variables are described in Table~\ref{tbl-pairwise-structure} in the
same order as the file columns. Prefixes \texttt{ID1\_} and
\texttt{ID2\_} identify the first and second signal in each comparison.

\begin{table}[hbt]

\caption{\label{tbl-pairwise-structure}Structure of the
\texttt{wire\_pairwise\_ccf.csv} file.}

\centering{

\begin{tabularx}{\linewidth}{llX}
\toprule
Variable & Type & Description \\
\midrule
\texttt{ID1\_tool} & text & Tool label for the first signal, one of T1 through T5 \\
\texttt{ID1\_edge} & character & Cutting edge for the first signal, one of A, B, C, or D \\
\texttt{ID1\_location} & text & Cutting location for the first signal: LI, LM, or LO \\
\texttt{ID1\_rep} & integer & Replicate for the first signal, either 1 or 2 \\
\texttt{ID1\_ID1} & text & Scan identifier for the first signal \\
\texttt{ID2\_tool} & text & Tool label for the second signal, one of T1 through T5 \\
\texttt{ID2\_edge} & character & Cutting edge for the second signal, one of A, B, C, or D \\
\texttt{ID2\_location} & text & Cutting location for the second signal: LI, LM, or LO \\
\texttt{ID2\_rep} & integer & Replicate for the second signal, either 1 or 2 \\
\texttt{ID2\_ID2} & text & Scan identifier for the second signal \\
\texttt{ccf} & numeric & Maximized cross-correlation function value for the aligned signals \\
\texttt{lag} & integer & Relative shift at which \texttt{ccf} is maximized, in profile-sample increments \\
\texttt{same\_tool} & logical & Whether both signals were produced by the same tool \\
\texttt{same\_edge} & logical & Whether both signals were produced by the same tool and edge \\
\texttt{same\_location} & logical & Whether both signals were produced by the same tool, edge, and location \\
\texttt{same\_source} & logical & Whether \texttt{same\_location} is true; replicates may differ \\
\texttt{ID1\_edge\_long} & logical & Whether the first edge is a long edge, A or C \\
\texttt{ID2\_edge\_long} & logical & Whether the second edge is a long edge, A or C \\
\texttt{edge\_type} & text & Edge-length pairing: Long, Short, or Mixed \\
\bottomrule
\end{tabularx}

\agentreview{Confirm whether the current pairwise column names, including \texttt{ID1\_ID1}, \texttt{ID2\_ID2}, \texttt{ID1\_rep}, and \texttt{ID2\_rep}, should be retained.}

}

\end{table}%

\section*{Data Overview}\label{sec-data-overview}
\addcontentsline{toc}{section}{Data Overview}

As descriptive summaries of comparison behavior across the dataset,
Figure~\ref{fig-ccf-tilemap} displays pairwise CCF values organized by
tool, cut surface, blade location, and replicate. The main diagonal
contains self-comparisons, whereas the off-diagonal cells compare
distinct signals. Figure~\ref{fig-ccf-density} displays the CCF
distributions for same-source and different-source comparisons.

\begin{figure}[hbt]

\centering{

\includegraphics[width=6.25in,height=\textheight,keepaspectratio]{images/ccf_tilemap.png}

}

\caption{\label{fig-ccf-tilemap}Pairwise CCF tile map. Rows and columns
group scans by tool, cut surface, blade location, and replicate. Orange
cells indicate larger CCF values and gray cells indicate smaller values;
the inset enlarges the boxed edge-to-edge comparison block.}

\end{figure}%

\begin{figure}[hbt]

\centering{

\includegraphics[width=5.20833in,height=\textheight,keepaspectratio]{images/ccf_density.png}

}

\caption{\label{fig-ccf-density}CCF density distributions. Kernel
density estimates are shown for different-source comparisons in gray and
same-source comparisons in orange.}

\end{figure}%

\section*{Technical Validation}\label{sec-technical-validation}
\addcontentsline{toc}{section}{Technical Validation}

Technical validation was conducted in two areas: (1) signal processing
and alignment and (2) variability assessment.

Scan processing and signal alignment were validated as follows. High CCF
values were expected between signals from the same source and low values
between signals from different sources.
Figure~\ref{fig-signals-pair-alignment}, shown earlier in Pairwise
alignment of signals, meets the expected pattern for same-source
signals. As an example of a different-source comparison, T1AW-LI-R1 and
T2AW-LI-R1 were selected.
Figure~\ref{fig-T1AW-LI-R1-T2AW-LI-R1-alignment} (a) and (b) show the
selected scans. Their signal alignment, shown in
Figure~\ref{fig-T1AW-LI-R1-T2AW-LI-R1-alignment} (c), has a CCF value of
0.2, which is as low as expected. For the following summaries, edges A
and C were categorized as long, edges B and D as short, and comparisons
pairing one long edge with one short edge as mixed. The resulting CCFs
for all pairwise comparisons were also summarized using boxplots and
receiver operating characteristic (ROC) curves.
Figure~\ref{fig-ccf-boxplot} groups CCF distributions by different edge,
different location, different tool, and same source, with the paired
edges identified on the horizontal axis. Figure~\ref{fig-ccf-ROC} shows
classification performance separately for long and short edge classes.
The ROC curves plot sensitivity, or the true positive rate, against the
false positive rate (FPR; 1 - specificity). The curves are close to the
upper-left corner, indicating strong classification performance. For the
combined comparison set, a threshold of 0.589 controls the FPR below
0.05 with a false negative rate (FNR) of 0, whereas a threshold of 0.658
controls the FPR below 0.01 with an FNR of 0.02. These results are
consistent with the expected pattern and validate the pipeline for
processing and aligning signals.

\begin{figure}[hbt]

\begin{minipage}[t]{0.50\linewidth}

\centering{

\includegraphics[width=\linewidth,height=5.5cm,keepaspectratio]{images/T1AW-LI-R1.png}

}

\subcaption{\label{fig-T1AW-LI-R1}}

\end{minipage}%
%
\begin{minipage}[t]{0.50\linewidth}

\centering{

\includegraphics[width=\linewidth,height=5.5cm,keepaspectratio]{images/T2AW-LI-R1.png}

}

\subcaption{\label{fig-T2AW-LI-R1}}

\end{minipage}%
\newline
\begin{minipage}[t]{0.05\linewidth}
~\end{minipage}%
%
\begin{minipage}[t]{0.90\linewidth}

\centering{

\includegraphics[width=\linewidth,height=5cm,keepaspectratio]{images/T1AW-LI-R1-T2AW-LI-R1.png}

}

\subcaption{\label{fig-T1AW-LI-R1-T2AW-LI-R1}}

\end{minipage}%

\caption{\label{fig-T1AW-LI-R1-T2AW-LI-R1-alignment}Different-source
scan and signal comparison. (a) Scan T1AW-LI-R1 cut by tool 1. (b) Scan
T2AW-LI-R1 cut by tool 2. (c) Alignment of signals from T1AW-LI-R1 and
T2AW-LI-R1.}

\end{figure}%

\begin{figure}[hbt]

\centering{

\includegraphics[width=1\linewidth,height=\textheight,keepaspectratio]{images/ccf_boxplot.png}

}

\caption{\label{fig-ccf-boxplot}CCF distributions across comparison
classes. Gray boxplots show different-source comparisons grouped by
different edge, location, or tool; orange boxplots show same-source
comparisons. Horizontal-axis labels identify the paired edges.}

\end{figure}%

\begin{figure}[hbt]

\centering{

\includegraphics[width=5.20833in,height=\textheight,keepaspectratio]{images/ccf_ROC.png}

}

\caption{\label{fig-ccf-ROC}ROC curves for CCF-based classification.
Curves are shown separately for long and short edge classes; the dotted
diagonal indicates chance-level classification.}

\end{figure}%

The second validation assessed variability in the pipeline across
replicates, stagings, and acquisitions. Scans from tool 2 at the middle
location were selected for this assessment. Replicate variability was
assessed using two different cuts at the same edge and position. The
comparison in Figure~\ref{fig-signals-pair-alignment} in Pairwise
alignment of signals illustrates this assessment. To assess staging
variability, replicate 2 was rescanned under the confocal microscope,
and the staging label S was introduced into the naming protocol. The S1
designation is omitted from labels, so R2 is equivalent to R2-S1. For
all four edges of tool 2 at the middle location, three stagings (R2,
R2-S2, and R2-S3) were created, pairwise CCFs between stagings were
computed, and their mean and standard deviation (SD) were calculated.

Within each staging, the effect of different acquisitions was further
examined by keeping the samples on the confocal microscope while varying
the lighting conditions. A was introduced as the acquisition label. For
edge A at staging 3, four acquisitions were performed, and the A1
designation was omitted from the first acquisition label. Therefore,
R2-S3 is equivalent to R2-S3-A1. For the other three edges, two
acquisitions were conducted at staging 3. These acquisitions are denoted
by R2-S3 and R2-S3-A2. Within staging 3, pairwise CCFs between
acquisitions were computed, and their mean and standard deviation were
calculated. Variability between distinct stagings was assessed
separately through the staging comparisons described above.

With this setup, CCF values were expected to become more consistent from
replicate to staging to acquisition as the scanning conditions became
progressively more stable. Consequently, variability was expected to
decrease along this sequence. Replicates, stagings, and acquisitions
were also compared, with smaller differences expected between
acquisitions and stagings than between stagings and replicates. The
results were visualized as scatter plots.
Figure~\ref{fig-rep_staging_acquisition} shows the scatter plots and
intervals extending two standard deviations above and below the mean.
Higher orange mean lines indicate an increase in the mean CCF, and
narrower shaded intervals indicate a decrease in the standard deviation.
The detailed numerical results, including means, standard deviations,
and differences between means, are shown in
Table~\ref{tbl-CCF-replicate-staging-acquisition} and confirm the
expected pattern.

\begin{figure}[hbt]

\centering{

\includegraphics[width=4.6875in,height=\textheight,keepaspectratio]{images/rep_staging_acquisition.png}

}

\caption{\label{fig-rep_staging_acquisition}Variability across scanning
conditions. Panels A, B, C, and D correspond to the four cut surfaces.
Gray points show pairwise CCF values, orange horizontal lines show
means, and shaded intervals extend two standard deviations above and
below each mean.}

\end{figure}%

\agentformat{The manuscript contains 11 figures.
Scientific Data recommends no more than eight but does not mandate this limit.
Confirm with the coauthors or professor whether any logically connected pairs should be combined for revised submission; do not remove a figure or its discussion without an author decision.}

\begin{table}

\caption{\label{tbl-CCF-replicate-staging-acquisition}Comparison of CCF
means and standard deviations across different replicates, stagings, and
acquisition settings. Dashes indicate that the standard deviation is
undefined because only one CCF value is available.}

\centering{

\centering
\begin{tabular*}{\linewidth}{@{\extracolsep{\fill}}lrrrrrrrr@{}}
\toprule
 & \multicolumn{2}{c}{\textbf{Replicate (R)}} 
 & \multicolumn{3}{c}{\textbf{Staging (S)}} 
 & \multicolumn{3}{c}{\textbf{Acquisition (A)}} \\
\cmidrule(lr){2-3} \cmidrule(lr){4-6} \cmidrule(lr){7-9}
\textbf{Edge} 
 & \textbf{Avg$_R$}
 & \textbf{SD$_R$}
 & \textbf{Avg$_S$}
 & \textbf{SD$_S$}
 & \textbf{Avg$_S$ - Avg$_R$} 
 & \textbf{Avg$_A$} 
 & \textbf{SD$_A$}
 & \textbf{Avg$_A$ - Avg$_S$} \\
\midrule
A & 0.818 & 0.015 & 0.961 & 0.012 & 0.143 & 0.992 & 0.003 & 0.031 \\
B & 0.879 & 0.031 & 0.922 & 0.042 & 0.043 & 0.939 & --- & 0.017 \\
C & 0.841 & 0.022 & 0.952 & 0.021 & 0.111 & 0.959 & --- & 0.007 \\
D & 0.797 & 0.011 & 0.966 & 0.017 & 0.169 & 0.986 & --- & 0.020 \\
\bottomrule
\end{tabular*}

}

\end{table}%

The variability assessment does not include variation in CCF values
caused by differences in the manual extraction of profiles from scans.
To support reproduction of the results reported here, the extraction
locations and values for each scan have been included.

\section*{Usage Notes}\label{sec-usage-notes}
\addcontentsline{toc}{section}{Usage Notes}

The raw surface scans are provided in \texttt{x3p} format. To inspect or
process these files in R, use the \texttt{x3ptools} package and its
reference manual, available from CRAN
\citep{hofmannX3ptoolsToolsWorking2018}. The supplied profiles and
processed signals provide a starting point for users who do not wish to
repeat manual profile extraction. The supplied extraction coordinates
allow these derived files to be related back to the corresponding scans.
Because each supplied signal is based on a manually selected profile,
users assessing sensitivity to profile placement should re-extract
profiles from the raw \texttt{x3p} scans using alternative documented
locations.

\section*{Data Availability}\label{sec-data-availability}
\addcontentsline{toc}{section}{Data Availability}

\agentrevision{The data described in this Data Descriptor are available from Iowa State University DataShare [DATASET CITATION] at \url{https://doi.org/[DOI]}.}
\agentformat{Replace the dataset citation and DOI placeholders with the same record used in Data Records.}

\section*{Code Availability}\label{sec-code-availability}
\addcontentsline{toc}{section}{Code Availability}

Custom R code was used to inspect raw scans, extract profiles, derive
signals, align signals, and generate the validation outputs. The code is
available in the \texttt{assessment/code} folder at
\url{https://github.com/heike/wirecuts-data}.
\agentformat{For permanent access, archive the exact code release in a DOI-providing repository and cite it if feasible; Scientific Data recommends combining GitHub with a DOI archive.}
The files and their roles are described in
Table~\ref{tbl-code-overview}.

\begin{table}

\caption{\label{tbl-code-overview}Overview of available code.}

\centering{

\begin{tabular*}{0.95\linewidth}{@{\extracolsep{\fill}}>{\raggedright\arraybackslash}p{\dimexpr 0.43\linewidth -2\tabcolsep-1.5\arrayrulewidth}|>{\raggedright\arraybackslash}p{\dimexpr 0.31\linewidth -2\tabcolsep-1.5\arrayrulewidth}>{\raggedright\arraybackslash}p{\dimexpr 0.21\linewidth -2\tabcolsep-1.5\arrayrulewidth}}
\toprule
Code & Description & Section \\ 
\midrule\addlinespace[2.5pt]
\multicolumn{3}{>{\raggedright\arraybackslash}m{0.95\linewidth}}{\parbox{\linewidth}{Inspect raw scans}} \\[2.5pt] 
\midrule\addlinespace[2.5pt]
\path{1-create_pngs_from_x3p.R} & obtain images of \texttt{x3p} files in \texttt{scans/} & Scan acquisition \\ 
\midrule\addlinespace[2.5pt]
\multicolumn{3}{>{\raggedright\arraybackslash}m{0.95\linewidth}}{\parbox{\linewidth}{Extract profiles}} \\[2.5pt] 
\midrule\addlinespace[2.5pt]
\path{2-create_profiles_from_x3p.R} & manually extract profiles from each scan & Signal extraction \\ 
\path{3-create-single-profile-file.R} & combine profile information & Signal extraction \\ 
\midrule\addlinespace[2.5pt]
\multicolumn{3}{>{\raggedright\arraybackslash}m{0.95\linewidth}}{\parbox{\linewidth}{Derive signals}} \\[2.5pt] 
\midrule\addlinespace[2.5pt]
\path{4-create_signals_from_profiles.R} & derive signals from each profile & Signal extraction \\ 
\midrule\addlinespace[2.5pt]
\multicolumn{3}{>{\raggedright\arraybackslash}m{0.95\linewidth}}{\parbox{\linewidth}{Compare signals}} \\[2.5pt] 
\midrule\addlinespace[2.5pt]
\path{5-create-images.R} & create images for pairwise alignment & Pairwise alignment of signals \\ 
\path{6-align-pairwise.R} & compute pairwise alignment CCF values & Pairwise alignment of signals \\ 
\path{7-all-comparison-results.R} & visualize comparison results & Data Overview and Technical Validation \\ 
\bottomrule
\end{tabular*}

}

\end{table}%

The provided code reproduces the processing, comparison, validation, and
visualization outputs described in Data Acquisition and Processing and
Technical Validation.
\agentrevision{The workflow was run using R [version] and \texttt{x3ptools} [version].
Versions of additional R packages are recorded in [environment file or repository documentation path].}
\agentformat{Replace the R, \texttt{x3ptools}, and environment-record placeholders after checking the analysis environment.}

\renewcommand{\bibsection}{\section*{\refname}\addcontentsline{toc}{section}{\refname}}
\bibliographystyle{naturemag-doi}
\bibliography{references}

\section*{Author Contributions}
\agentformat{Confirm whether all authors approve conversion of this statement to the CRediT taxonomy before revising the contributor roles.}

Y.L.: Methodology, Software, Validation, Data Curation, Writing: Draft;
H.H.: Conceptualization, Methodology, Validation, Writing: Review and
Editing; C.M.: Laboratory Supervision; E.A.: Physical Specimen,
Scanning; J.S.: Forensic Advice; A.C.: Funding Acquisition.

All authors reviewed the manuscript.

\section*{Competing Interests}
H.H. is a technical advisor to the Association of Firearm and Toolmark
Examiners (AFTE), a fellow of the American Statistical Association
(ASA), and a member of the ASA Forensic Science Committee. H.H. has
testified as a court witness on behalf of Judge April Neubauer in the
New York State Supreme Court, Criminal Term, in New York City.
\agentformat{Confirm that the declaration covers every author and replace Alicia's declaration, if any.}
The authors declare the following competing interests:


\section*{Funding}
This work was partially funded by the Center for Statistics and
Applications in Forensic Evidence (CSAFE) through Cooperative Agreement
70NANB20H019 between NIST and Iowa State University. The agreement
includes activities carried out at Carnegie Mellon University, Duke
University, the University of California, Irvine, the University of
Virginia, West Virginia University, the University of Pennsylvania,
Swarthmore College, and the University of Nebraska-Lincoln.

\end{document}
